You receive two transcripts in {language}.

REFERENCE TRANSCRIPT (reference transcript):
"{reference_text}"

NORMALIZED TRANSCRIPT (text with normalization):
"{text}"

TASK: In the NORMALIZED transcript, align named entities with the REFERENCE transcript.
Replace only entity spellings (names, places, organizations, titles) with the REFERENCE form.
Keep all other text from the NORMALIZED transcript exactly as written.

OUTPUT: Return ONLY the corrected normalized text—no explanations, JSON, or labels.
